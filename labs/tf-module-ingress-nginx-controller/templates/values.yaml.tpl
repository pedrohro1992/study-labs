controller:
  ingressClassResource:
    name: nginx
    # Mover controller.ingressClassResource.name pra implementar uma logica que chega se o cluster vai ter um ingress-internal, ingress-external ou os dois